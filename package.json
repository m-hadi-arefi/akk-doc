{
  "name": "randdakk",
  "version": "1.0.0",
  "description": "> **A knowledge base for gradually modernizing the software ecosystem and enterprise architecture of Arian Khodro.**",
  "license": "ISC",
  "author": "mhadi",
  "type": "module",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "dev": "vitepress dev --host 0.0.0.0 --port 3000",
    "build": "vitepress build && node -e \"import('node:fs').then(fs => { if (fs.existsSync('.vitepress/dist')) fs.cpSync('.vitepress/dist', 'dist', { recursive: true, force: true }); });\"",
    "preview": "vitepress preview --host 0.0.0.0 --port 3000",
    "docs:dev": "vitepress dev --host 0.0.0.0 --port 3000",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview --host 0.0.0.0 --port 3000",
    "docs:check-sync": "node scripts/check-docs-sync.mjs",
    "lint": "node scripts/check-docs-sync.mjs"
  },
  "devDependencies": {
    "postcss-rtlcss": "^6.0.0",
    "vitepress": "^2.0.0-alpha.19"
  }
}
